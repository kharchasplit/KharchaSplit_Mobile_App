import { View, Text } from 'react-native'
import React from 'react'
import { ModalScreenProps } from '../types/navigation'

const EditProfileScreen: React.FC<ModalScreenProps> = ({ onClose }) => {
  return (
    <View>
      <Text>EditProfileScreen</Text>
    </View>
  )
}